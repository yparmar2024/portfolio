/**
 * Timing constants for animations, transitions, and delays
 * Centralized to prevent magic numbers throughout the codebase
 */

export const TIMINGS = {
  DAMAGE_OVERLAY_DURATION: 300,
  SERVER_REFRESH_DELAY: 800,
  MUSIC_FADE_INTERVAL: 100,
  WORLD_LOADING_DELAY: 800,
};
